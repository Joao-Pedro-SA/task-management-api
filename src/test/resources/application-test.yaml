spring:
  application:
    name: AgendaDeAtividades
  datasource:
    url: "jdbc:h2:mem:agenda"
    username: "sa"
    password: ""

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true